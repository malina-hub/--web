# 银河官网设计规范（第一版）

本文档是后续页面搭建的视觉基准，数值来自首页与「银河光影嘉年华」当前落地代码。新增页面优先复用全局 Token 和已有组件，不要单独创造新的字号、版心或间距。

## 主题分层

### 深色视觉页面

- 首页 `#/`
- 银河光影嘉年华 `#/products/carnival`
- 产品矩阵聚合页 `#/products`
- 解决方案总览页 `#/solutions`

背景以 `#14171f` 为主，内容卡片使用深蓝灰表面和蓝色描边，正文白色/白色透明度，强调色使用银河蓝 `#1873ed`、`#80adf9` 和蓝紫渐变。

### 浅色视觉页面

- 产品矩阵各产品详情页
- 解决方案各产品详情页
- 关于银河页

浅色页面统一使用 `#ffffff` 页面背景、`#ffffff` 卡片、`#14171f` 主文字、`#657083` 次文字和 `#1873ed` 强调色。后续实现详情页时，使用页面级主题类切换颜色，不要复制一套结构。

### 智慧大屏浅色详情页补充

Figma 设计稿节点：`219:4894`（产品矩阵 / 智慧大屏），本地路由为 `#/products/aggregation`。Figma 原稿结构为全局导航 `60px`、Hero `490px`、四大核心能力 `1808px`、典型应用场景 `456px`、深色 CTA/Footer `647px`；按已确认的产品详情页统一规范，实际实现 Hero 高度调整为 `500px`。

智慧大屏是浅色产品详情页，页面背景为 `#fff`，主文字为 `#14171f`，说明文字为 `#666`，强调色为 `#1873ed`。Hero 使用 Figma 导出的浅蓝渐变素材，标题 `52/60px`、描述 `16/28px`，标题与描述视觉间距 `20px`，描述与按钮组间距 `40px`，两个按钮间距 `16px`。Hero 背景图按 `100% × 130.03%` 展开并上移 `23.57px`，避免使用会改变裁切结果的 `cover`。

四大核心能力区域使用 `1000px` 版心：模块标题 `40/48px`、副文本 `16/28px`、标题区与内容间距 `40px`。四组能力内容均为 `400px` 文案 + `40px` 间隔 + `560 × 315px` 产品截图，组间距 `80px`，左右交替排列。文案标题为 `28/40px`，描述为 `16/26px`，标题/描述间距 `8px`，能力要点与按钮区域间距 `44px`，详情按钮高度 `36px`、字号 `14px`、字重 `500`。

典型应用场景区域高度 `456px`，使用 Figma 渐变背景素材，标题 `40/48px`，标题与卡片间距 `40px`。四张卡片宽 `238px`、高 `188px`、间距 `16px`、圆角 `16px`，卡片内边距 `28px 20px`，标题字号 `20/30px`。

底部 CTA/Footer 沿用已定稿的深色结构：背景 `#14171f`，CTA 顶部留白 `80px`，标题 `48px`、字重 `500`，标题与按钮间距 `32px`；Footer 版心 `1000px`，分隔线与内容间距 `32px`，底部留白 `100px`。页面素材统一保存在 `src/assets/figma-pages/smart-screen/`，实现文件为 `src/pages/SmartScreenPage.jsx`、`src/pages/SmartScreenPage.css`。

### 产品矩阵深色聚合页补充

Figma 设计稿节点：`120:138`，本地路由为 `#/products`。页面基准尺寸为 `1440 × 4291px`，固定导航 `60px`；首屏 `790px`，大屏与泛屏产品区 `830px`，AI/数据/商业能力区 `1134px`，银河品牌业务区 `830px`，底部 CTA/Footer `647px`。

聚合页沿用首页与银河嘉年华的深色视觉：页面背景 `#14171f`，内容版心 `1000px`，主标题 `60/60px` 渐变字，模块标题 `40/48px`，模块副文本 `16/28px`。首屏多屏视觉由左侧倾斜泛屏、中央智慧大屏和右侧智能推荐三张产品图组成，中心图尺寸约 `540 × 304px`，侧图约 `310 × 175px`。

产品图文卡片统一使用 `20px` 圆角、深蓝渐变底、`#354b7e` 描边；双列产品卡宽 `488px`、高 `522px`，图片区 `448 × 256px`。AI/数据/商业能力区使用一张横向大卡和四张双列能力卡，能力卡正文 `14/24px`，按钮右对齐。页面素材保存在 `src/assets/figma-pages/products-aggregation/`，实现入口在 `src/pages/FrameworkPage.jsx` 与 `src/pages/FrameworkPage.css`。

### 解决方案聚合页补充

Figma 设计稿节点：`120:166`，本地路由为 `#/solutions`。页面沿用深色视觉，导航占位 `60px`；首屏使用 `1000px` 版心，标题 `60px` 渐变字，三张统计卡宽约 `317px`、高 `240px`、间距 `24px`。方案列表采用四行交替布局，媒体区约 `500 × 340px`，文案区与媒体间距 `28px`，行间距 `80px`。

方案卡片文案与客户类型以产品经理 Demo 为准，卡片标签高度 `24px`，使用无边框的半透明银河蓝底和白色文字；关键指标使用标题渐变色。聚合页不单独增加中间 CTA，底部统一 Footer 的 CTA 使用“共同探索更多业务场景”文案；详情路由继续保留在 `#/solutions/smarttv`、`#/solutions/homeaudio`、`#/solutions/midscreen`、`#/solutions/autoscreen`。

### 本轮已确认的全局优化项

- 导航背景统一为 `#14171f`，五个一级入口默认字号 `14px`、字重 `500`、行高 `20px`。
- 导航默认文字为 `#d6dbe4`，hover、展开和当前项文字为 `#fff`，当前项不额外加粗；有二级菜单的入口保留统一的下拉箭头和底部白色指示线。
- 浅色详情页 Banner 位于导航下方，导航占位 `60px`，避免固定导航覆盖首屏内容。
- 浅色 Banner 主次按钮均为 `40px` 高、`8px` 圆角、`14px/500`；按钮组间距按首页规范使用 `12px`，次按钮 hover 为纯白背景、白色边框、蓝色文字。
- 浅色详情页内部“咨询产品”按钮去除边框，使用 `14px/500`；底部“咨询商务”按钮与首页 CTA 的尺寸、渐变和 hover 反馈保持一致。
- Figma 提供的 Logo、产品截图、图标和背景素材优先下载到本地后引用，不使用过期远程资源或手写占位图形。

## 布局与版心

- 桌面内容版心：`1000px`
- 页面最小左右安全边距：`24px`
- 页面容器：`width: min(1000px, calc(100% - 48px))`
- 首页 Hero：桌面高度 `760px`
- 嘉年华 Hero：最小高度 `700px`
- 模块顶部间距：`80px`
- 模块底部间距：`100px`
- 模块标题与副文本间距：`12px`
- 模块标题与内容网格间距：`40px`（复杂列表可使用 `48px` 或 `56px`）
- 卡片网格常用间距：`16px`；双列大卡片使用 `24px`

移动端将版心改为 `calc(100% - 32px)`，模块上下间距收窄到 `64px / 72px`，标题字号按页面断点缩小。

## Banner 规范

### 标题

- 桌面：`60px / 60px`，字重 `600`
- 首页标题第二行使用浅青到银河蓝渐变；嘉年华标题沿用同一标题尺寸与居中布局
- 标题多行时行间视觉间距 `16px`
- 标题区域和描述、按钮整体垂直居中

### 描述

- 最大宽度：`796px`
- 字号：`16px`
- 行高：首页 `30px`；详情 Banner 可使用 `28px`，但同一页面保持一致
- 颜色：深色页面 `rgba(255,255,255,.8)`
- 文字必须居中，容器高度随行数自适应，不写固定高度

### Banner 按钮

- 高度：`40px`
- 圆角：`8px`
- 字号：`14px`
- 字重：`500`
- 按钮组间距：`12px`
- 主按钮：银河蓝渐变，白色文字
- 次按钮：半透明白底、白色半透明描边和文字
- Hover：整体上移 `2px`；主按钮提高亮度；次按钮切换为白底、深色文字
- Focus：`2px` 蓝色可见焦点描边，外扩 `3px`
- Hover 弹层必须使用高于 Banner 的层级，并允许溢出 Banner，不被 Hero `overflow` 裁切
- 产品矩阵与解决方案的各产品详情页 Banner：副文本与按钮组之间固定为 `40px`，标题与副文本间距按页面设计稿单独定义；不得沿用旧的 `60px` 间距。
- 产品矩阵与解决方案的各产品详情页 Banner 高度统一为 `500px`。
- 全局固定导航占位 `60px`；详情页 Banner 必须从导航下方开始，使用 `margin-top:60px`（或等效布局），不得被导航覆盖。
- 产品矩阵与解决方案的各产品详情页 Banner 内容区域在 Banner 内垂直居中；标题、副文本和按钮组作为整体居中，不因固定上下内边距造成视觉偏移。

## 模块标题与副文本

- 模块标题：`40px / 48px`，字重 `700`，居中
- 模块副文本：`16px / 30px`，常规字重，居中
- 副文本颜色：深色页面 `rgba(255,255,255,.8)`；浅色页面使用 `#657083`
- 标题和副文本高度随内容自适应，禁止固定高度
- 首页 `.metrics`、`.story`、`.partners` 与嘉年华 `.section-heading` 使用同一规范

## 卡片与交互

- 常用圆角：`16px`
- 嘉年华城市卡片圆角：`20px`
- 深色卡片描边：`#354b7e` 或对应透明度
- Hover 通用反馈：上移 `4px`，描边变亮；不改变内容尺寸
- 图标与导航第一行文字水平对齐，二级导航图标标准为 `20×20px`

## 底部 CTA 与 Footer

深色页面统一复用首页 Footer 结构：

- CTA 顶部留白：`80px`
- CTA 标题：`48px / 1.2`，字重 `500`，居中
- CTA 标题与按钮间距：`32px`
- CTA 按钮高度：`40px`，圆角 `8px`
- Footer 内容版心：`1000px`
- Footer 分隔线距内容：`32px`
- Footer 联系信息字号：`14px`，行高约 `26–32px`
- Footer 底部留白：`100px`

浅色详情页可复用相同结构，但将背景、文字、分隔线和 CTA 颜色切换为浅色主题 Token。

### 产品矩阵卡片补充

- Banner 主标题渐变统一沿用银河光影嘉年华 Banner：`linear-gradient(134deg, #d2fffe 22%, #50a5ff 70%)`。
- 产品卡片标签高度 `24px`，字号 `14px`，标签间距 `6px`，不使用边框，背景使用 `rgba(24,115,237,.4)`，文字为白色。
- 产品卡片标签按产品能力拆分为三个横向标签；能力区产品图标为 `28×28px`，产品与品牌卡片图标为 `24×24px`。
- CTA 前置模块使用首页「与你熟悉的品牌，共创更好的大屏体验」的背景切图，作为模块底部氛围背景。
- 全项目统一复用首页 Footer 结构、文案、CTA 和联系信息。

## 全局 Token 位置

当前 Token 定义在 `src/index.css` 的 `:root`：

`--content-width`、`--page-gutter`、`--hero-title-size`、`--hero-title-line-height`、`--module-title-size`、`--module-title-line-height`、`--module-subtitle-size`、`--module-subtitle-line-height`、`--section-padding-top`、`--section-padding-bottom`、`--section-content-gap`、`--button-height`、`--button-radius` 以及深浅主题颜色 Token。

## 后续开发检查清单

1. 先确定页面主题：`dark` 或 `light`。
2. 使用 `1000px` 版心和 `24px` 最小左右边距。
3. Banner 标题、描述、按钮按本规范实现，文本高度随内容自适应。
4. 模块标题和副文本复用 40/48、16/30 规范。
5. 模块间距优先使用 80/100 和 40。
6. 深色页面 Footer 直接复用首页规范；浅色页面只切换主题 Token。
7. 完成后执行 `pnpm build`，并在桌面、移动端各检查一次。
